# Analysis (Archival Only)

**This directory is NOT compiled or included in the build.** It is excluded via `tsconfig.json`.

The files here are archival reference copies and design notes that duplicate
live gameplay code. Do **not** edit these files expecting changes to affect
the running game — the live implementations live under `src/combat`,
`src/audio`, etc.
